// import { useDispatch } from "react-redux";
// import { clearState as clientOrderSliceClear } from "../slices/clientOrderSlice.ts";
// import { clearState as companySliceClear } from "../slices/companySlice.ts";
// import { clearState as mainSliceClear } from "../slices/mainSlice.ts";
// import { clearState as managerSliceClear } from "../slices/managerSlice.ts";
// import { clearState as orderSliceClear } from "../slices/orderSlice.ts";
// import { deleteAllTimers as timerSliceClear } from "../slices/timerSlice.ts";
// import { clearState as userSliceClear } from "../slices/userSlice.ts";

const clearState = () => {
  console.log(1);
  // const dispatch = useDispatch();
  console.log(2);
  // localStorage.setItem("persist:root", "");
  //
  // dispatch(mainSliceClear());
  // dispatch(companySliceClear());
  // dispatch(clientOrderSliceClear());
  // dispatch(managerSliceClear());
  // dispatch(orderSliceClear());
  // dispatch(timerSliceClear());
  // dispatch(userSliceClear());
};

export default clearState;
